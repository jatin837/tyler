pub struct Token {
    TYPE: TokenType,
    lexeme: String,
    literal: Literal,
    line: i32, 
  
  }
pub impl Token {
      to_string(&self){
          //return self.Type + " " self.lexeme + " " + self.literal
      }
  }